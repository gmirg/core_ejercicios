function es_primo(num){
    var i=2;
    while(num%i != 0){ // mientras que el resto de divir el parametro introducido entre la iteración sea distinto de cero, seguimos con la iteracion
    }
    debugger;
    return i==num;
}
